# Open file cypress.json and update env:
  ```yaml
    baseUrl: YOUR_APP_URL
    token: APP_TOKEN
  ```
# Install package
 ```yaml
    yarn or npm install
 ```
# Run test:
```yaml
    yarn cy:open or yarn cy:test
```
